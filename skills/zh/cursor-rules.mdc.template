---
description: "Sopify protocol entry for Cursor Agent"
alwaysApply: true
---
<!-- SOPIFY_VERSION: 2026-07-21 -->

# Sopify Cursor 项目入口

这是 Cursor IDE 与 Cursor Agent CLI 共用的项目级规则。它负责路由，不替代各个 Skill 的详细内容。

## 每次 Sopify 请求的入口

1. 先读取 `{{skills_root}}/references/shared-writing-dna.md`，再输出任何 Sopify 回复；不要只说“参考过”。
2. 先分类意图：`consult_readonly`、`quick_fix`、`new_plan`、`continue_plan` 或 `finalize`。
3. `consult_readonly` 读取本规则与共享写作规范后直接回答，不加载阶段 Skill；`consult_readonly` 与 `quick_fix` 不自动接续旧方案，不写 `.sopify/state/`、`receipts/` 或知识库；需要只读时保持只读。
4. 需要进入工作流时，读取并执行下面列出的 `~/.cursor` 精确路径；不得替换为 `~/.claude`、`~/.codex` 或其他兼容目录中的同名 Skill：
   - 需求分析：`{{skills_root}}/analyze/SKILL.md`
   - 方案设计：`{{skills_root}}/design/SKILL.md`
   - 开发实施：`{{skills_root}}/develop/SKILL.md`
   - 知识库：`{{skills_root}}/kb/SKILL.md`
   - 模板：`{{skills_root}}/templates/SKILL.md`

## 分析门与 managed 入口

- `analyze` 必须先读 `analyze/SKILL.md`，并调用其中现有的 `scripts/score_requirement.py` 执行确定性评分门；完整性评分低于 `workflow.require_score`（默认 7）时先追问，不物化方案。项目与全局配置均未显式设置 `auto_decide` 时按 `false` 处理。
- `new_plan`、`continue_plan`、`finalize` 或明确的 managed develop，才进入四步读链：
  `state/active_plan.json` → `plan/<id>/plan.md` → `state/current_handoff.json` → `plan/<id>/receipts/`。
- machine truth 只能经已安装 payload 中的 `sopify_writer` 库 API 写入；不要虚构 `python3 -m sopify_writer`，不要直接编辑 state、handoff 或 receipt。

## 支持边界与输出

- Cursor 首发为 `BASELINE_SUPPORTED`：安装文件和技能可发现性不等于 Cursor 已遵守协议；未有 IDE 与 CLI 黑盒证据时不得宣称 `PROTOCOL_VERIFIED`。
- 每次输出保持精简：`[{BRAND_NAME}] {阶段名} {状态符}`，随后给核心结果，最后给 `Changes:` 区块和作为最后一行的 `Next:`。
- 不修改 Cursor settings、代理、模型、账号、API Key 或钥匙串；不启用 Cloud Agent。
